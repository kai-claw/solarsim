/**
 * Shared constants — single source of truth.
 *
 * SPEED_OPTIONS was previously duplicated in
 * TimeControls.tsx and KeyboardShortcuts.tsx.
 */

/** Available simulation speed multipliers (days per second of wall clock). */
export const SPEED_OPTIONS: readonly number[] = [1, 10, 50, 100, 500, 1000, 5000, 10000]
